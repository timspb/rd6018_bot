# RD6018 Smart AI Controller: Telegram Edition

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://python.org)
[![Aiogram](https://img.shields.io/badge/Aiogram-3.x-green.svg)](https://aiogram.dev)
[![Home Assistant](https://img.shields.io/badge/Home%20Assistant-API-orange.svg)](https://home-assistant.io)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

Система управления лабораторным блоком питания **RD6018** для автоматической и ручной зарядки свинцово-кислотных АКБ (Ca/Ca, EFB, AGM) через **Home Assistant** с управлением в **Telegram**: многоэтапные алгоритмы, триггеры переходов, защиты и AI-анализ.

---

## 🚀 Основные возможности

- **Графики в реальном времени** — напряжение, ток, температура в Telegram с автообновлением
- **Автозаряд по профилям** — Ca/Ca, EFB, AGM с десульфатацией, Mix и безопасным финишем
- **Ручной режим (Custom)** — свои уставки Main/Mix, дельта-триггер и лимит по времени
- **Логи событий** — старт/завершение этапов, ёмкость на этапе, триггеры (└) и параметры переходов
- **AI-консультант** — анализ телеметрии через DeepSeek (фаза, аномалии, прогноз)
- **Восстановление сессии** — продолжение заряда после перезапуска (charge_session.json), уставки и таймеры из сессии, коррекция по потере связи и uptime прибора
- **Контроль температуры** — перед стартом (не ниже 10°C), во время заряда (35/40/45°C)
- **Быстрая установка уставок** — в чате два числа через пробел (напр. `16.50 1.4`) → напряжение и ток на RD6018
- **Выключение по условию (off)** — по достижению напряжения/тока или таймеру (в чате: `V>=`/`V<=`, `I>=`/`I<=`, H:MM); состояние сохраняется в `manual_off_state.json` и переживает перезапуск бота; кнопка **⏹ Off по условию** в меню **⚙️ РЕЖИМЫ**, статус и остаток времени на дашборде

---

## 📋 Логика автозаряда

### Этапы (FSM)

| Этап | Описание |
|------|----------|
| **Подготовка** | Мягкий старт 12.0В/0.5А до достижения ~12В на клеммах |
| **Main Charge** | Основная фаза: целевое U по профилю (14.4–15.0В), ток до лимита (CC → CV) |
| **Десульфатация** | При «застревании» тока: 16.3В/2% Ah до 2 ч (Ca/EFB — до 3 циклов, AGM — до 4) |
| **Mix Mode** | Финальный подъём (16.5В и т.п.), выход по дельта-V/дельта-I или лимиту времени |
| **Безопасное ожидание** | Выход выключен, ожидание падения напряжения до целевого −0.5В (макс 2 ч) |
| **Done** | Режим хранения (13.2–13.8В) или полное отключение |
| **🌡 Остывание** | При 40°C: пауза заряда до снижения до 35°C |

### Профили (уставки и лимиты)

- **Ca/Ca** — Main 14.7В/5А, Mix до 8 ч, хранение 13.8В  
- **EFB** — Main 14.4В/5А, Mix до 10 ч, хранение 13.8В  
- **AGM** — 4 ступени 14.4→14.6→14.8→15.0В (на каждой ступени и перед Mix: ток &lt;0.2А в течение **2 ч** без нового минимума), затем Mix до 5 ч  

### Триггеры переходов (авто)

- **Подготовка → Main:** напряжение на АКБ ≥ 12.0В  
- **Main → Mix (Ca/EFB):** режим CV и ток &lt;0.3А в течение **3 ч** без нового минимума  
- **Main → Mix (AGM):** на всех ступенях до 15В и перед переходом в Mix — ток &lt;0.2А в течение **2 ч** без нового минимума  
- **Main → Десульфатация:** ток «застрял» ≥0.3А (Ca/EFB) или ≥0.2А (AGM) **≥40 мин**  
- **Mix → Безопасное ожидание:** срабатывание **дельта-триггера** (dV или dI) и затем **таймер 2 ч**  
- **Десульфатация → Безопасное ожидание:** ровно **2 ч** на этапе  
- **Безопасное ожидание → Main/Done:** напряжение упало до (целевое − 0.5В) или таймаут 2 ч  

Дельта-триггер в Mix: спад напряжения от пика ≥0.03В или рост тока от минимума ≥0.03А, подтверждённые **3 замерами подряд** с интервалом 1 мин. После смены уставок мониторинг dV/dI включается через **120 с** (исключение переходных процессов).

---

## 🛠 Логика ручного заряда (Custom)

### Что задаётся пользователем

1. **Напряжение Main** (12.0–17.0В) — целевое напряжение основной фазы  
2. **Ток Main** (0.1–18.0А) — лимит тока (при 10.1–18А запрашивается подтверждение)  
3. **Дельта (dV/dI)** — порог срабатывания финиша (по умолчанию 0.03В)  
4. **Лимит по времени** (1–72 ч) — максимальная длительность этапа Main  
5. **Ёмкость АКБ (Ah)** — для отображения и логики (не для жёсткого отключения по Ah)  

### Поведение

- Старт сразу с этапа **Main** (без Подготовки), уставки RD6018 = Main V/I  
- Выход из Main только по **дельта-триггеру** (те же 0.03В/0.03А, 3 подтверждения с интервалом 1 мин) или по **лимиту времени**  
- При достижении лимита времени: если уже CV и ток ≤ порога (0.3А) — переход в Mix; иначе — принудительное завершение (Done)  
- OVP/OCP выставляются автоматически (U_target+0.5В, I_limit+0.5А)  

Перед стартом (как и в авто) проверяется **внешняя температура**: при &lt;10°C заряд не запускается.

---

## 📝 Команды в чате (вне ручного режима)

### Быстрая установка уставок

Введите в чат **два числа через пробел** — напряжение (В) и ток (А). Бот выставит их на RD6018 и подтвердит.

- Пример: `16.50 1.4` → 16.50 В, 1.4 А  
- Диапазоны: напряжение 12–17 В, ток 0.1–18 А  
- После отправки бот проверяет сущности HA и сообщает: «На приборе: X.XX В | X.XX А» или предупреждение, если значения на приборе не совпадают.  
- Не срабатывает, если вы в диалоге выбора режима или ввода ёмкости. В ручном режиме на шаге «напряжение» можно сразу ввести пару `16.50 1.4` — примутся оба значения и переход к шагу «дельта».

### Выключение по условию (off)

Команда **`off`** задаёт принудительное выключение выхода при достижении **любого** из условий (игнорируя этап автозаряда). Защиты **не сбрасываются и не выставляются**; температура и входное напряжение по-прежнему работают и могут выключить выход раньше.

**Синтаксис:**

| Команда | Условие выключения |
|--------|---------------------|
| `off I<=1.23` или `off 1.23` | Ток ≤ 1.23 А |
| `off I>=2` | Ток ≥ 2 А |
| `off V>=16.4` или `off 16.4` | Напряжение ≥ 16.4 В |
| `off V<=13.2` | Напряжение ≤ 13.2 В (напр. в миксе) |
| `off 2:23` | Через 2 ч 23 мин от момента ввода |
| `off I>=2 V<=13.5 2:00` | При **любом** из условий (ток, напряжение, время) |
| `off` | Сброс условия выключения |

- Кнопка **⏹ Off по условию** находится в меню **⚙️ РЕЖИМЫ** (не на дашборде) — открывает статус и подсказку по синтаксису. В чате вводите только **`<=`** и **`>=`** (удобно на мобильном); в ответах бота для краткости выводятся символы ≤ и ≥.  
- Условие off **сохраняется в файл** `manual_off_state.json` и **восстанавливается после перезапуска** бота; таймер продолжает отсчёт в реальном времени.  
- На дашборде (краткая и полная инфо) отображается строка **⏹ ВЫКЛ ПО УСЛОВИЮ** и, при таймере, **осталось до выкл: H:MM**.  
- При нажатии «Остановить» или срабатывании условия условие off сбрасывается.

---

## ⚡ Триггеры и условия

| Триггер | Условие | Действие |
|--------|--------|----------|
| **V ≥ 12В** | Напряжение на клеммах после Подготовки | Подготовка → Main |
| **I &lt; 0.3А (3 ч)** | Ca/EFB: ток держится ниже порога 3 ч без нового минимума | Main → Mix |
| **I &lt; 0.2А (2 ч)** | AGM: на каждой ступени и перед Mix — без нового минимума | Main → следующая ступень или Mix |
| **I застрял 40 мин** | Ток ≥0.3А (или ≥0.2А AGM) в CV не падает 40 мин | Main → Десульфатация |
| **Дельта V ≥ 0.03В** | В Mix: спад от пика напряжения, 3×1 мин | Запуск таймера 2 ч → затем Safe Wait |
| **Дельта I ≥ 0.03А** | В Mix: рост от минимума тока, 3×1 мин | То же |
| **Таймер 2 ч** | После срабатывания дельты в Mix прошло 2 ч | Mix → Безопасное ожидание |
| **Лимит Mix** | 8 ч (Ca), 10 ч (EFB), 5 ч (AGM) | Mix → Безопасное ожидание |
| **Лимит Main 72 ч** | Защита от зависания (авто) | При I≤порога → Mix, иначе Done |
| **Лимит времени Custom** | Пользовательский лимит Main | Аналогично: Mix или Done |
| **V упало** | В Безопасном ожидании U ≤ целевое−0.5В | Включение выхода, переход в Main или Done |
| **Таймаут Safe Wait 2 ч** | Напряжение не упало за 2 ч | Принудительный переход дальше |

После каждой смены этапа/уставок действует **бланкинг 5 мин** — триггеры по току/напряжению не обрабатываются, чтобы избежать ложных срабатываний.

---

## 🛡 Защиты

### Перед стартом

- **Температура внешнего датчика &lt; 10°C** — заряд не запускается (авто и Custom). Сообщение: «Прогрейте АКБ или помещение».

### Температура во время заряда (внешний датчик АКБ)

- **35°C** — предупреждение в Telegram (один раз за сессию)  
- **40°C** — выход выключен, этап **🌡 Остывание**; возврат к заряду при снижении до 35°C  
- **45°C** — аварийный сброс в Idle, полная остановка, уведомление  

### Аппаратные и программные лимиты

- **OVP/OCP** — при каждой смене этапа: OVP = U_target + 0.5В, OCP = I_limit + 0.5А  
- **Максимальное напряжение** — ограничение в конфиге (например 17.5В)  
- **Watchdog связь с HA** — потеря связи &gt;3 мин → автоотключение выхода  
- **Watchdog RD6018** — нет данных &gt;5 мин (или &gt;1 мин при U&gt;15В) → принудительное OFF  

### Дополнительно

- **Phantom charge** — если ток упал ниже порога за &lt;10 мин, в лог и уведомление добавляется предупреждение о возможной сульфатации/потере ёмкости  
- **Валидация Custom** — проверка диапазонов V/I, дельты, лимита времени; для тока 10.1–18А — подтверждение  

---

## 📱 Интерфейс

### Скриншоты

**Дашборд (краткая инфо):** график U/I, статус (в т.ч. ⏱ время заряда, синхронизированное с таймером прибора по `sensor.rd_6018_uptime`), LIVE (V, I, T, CC/CV), при заданном условии off — строка «⏹ ВЫКЛ ПО УСЛОВИЮ» и остаток времени; кнопки Обновить, Полная инфо, AI Анализ, Логи, Стоп, **⚙️ РЕЖИМЫ** (внутри — профили и **⏹ Off по условию**).

![Дашборд](assets/dashboard.png)

**Полная информация по режиму:** тот же график, расширенный блок (этап, уставки, условие финиша ΔV/время, ёмкость) и те же кнопки.

![Полная инфо](assets/fullinfo.png)

**Выбор профиля заряда (Авто):** предупреждение о напряжении до 16.5В и отключении АКБ от бортсети, кнопки Ca/Ca, EFB, AGM, «Ручной режим», **⏹ Off по условию**, Назад.

![Режимы](assets/modes.png)

### Дашборд (график + подпись + кнопки)

- Под графиком: строка статуса (режим, тип АКБ, время) и строка LIVE (V, I, T, CC/CV).  
- **🔄 ОБНОВИТЬ** — обновить дашборд (график + краткая инфо).  
- **📋 Полная инфо** — тот же график + полный текст (этап, уставки, условие перехода, ёмкость, при заданном off — строка условия и остаток времени) и те же кнопки.  
- **🧠 AI АНАЛИЗ**, **📝 ЛОГИ СОБЫТИЙ**, **Старт/Стоп**, **⚙️ РЕЖИМЫ** (в подменю — профили Ca/Ca, EFB, AGM, Ручной режим и **⏹ Off по условию**).

### Логи событий

- Для каждого этапа: **старт** (START, ёмкость/профиль), при необходимости **└** строки по триггерам, при **завершении** — время на этапе, ёмкость на этапе, T, V, I в момент перехода и значение триггера.

---

## 🔧 Установка и настройка

### Требования

- Python 3.10+  
- Home Assistant с интеграцией RD6018  
- Telegram Bot Token  
- DeepSeek API Key (опционально, для AI)  

### Быстрый старт

```bash
git clone https://github.com/timspb/rd6018_bot.git
cd rd6018_bot
pip install -r requirements.txt
cp .env.example .env
# Отредактируйте .env: TG_TOKEN, HA_URL, HA_TOKEN и при необходимости DEEPSEEK_API_KEY, USER_TIMEZONE, ALLOWED_CHAT_IDS
python bot.py
```

### Docker

```bash
docker build -t rd6018-bot .
docker run -d --env-file .env rd6018-bot
```

### Основные переменные (.env)

```env
TG_TOKEN=...
HA_URL=http://homeassistant:8123
HA_TOKEN=...
DEEPSEEK_API_KEY=...
USER_TIMEZONE=Europe/Moscow
# ALLOWED_CHAT_IDS=123,456  # пусто = доступ у всех
```

### Сущности Home Assistant (RD6018)

- Напряжение на АКБ, выходной ток, внешняя температура, состояние выхода  
- Управление: выключатель выхода, уставки напряжения и тока, при наличии — OVP/OCP  

(Точные имена entity задаются в `config.py` / ENTITY_MAP.)

### Восстановление сессии и таймеры

- После восстановления (потеря связи, перезапуск бота, кнопка «ЗАПУСТИТЬ») уставки **берутся из сохранённой сессии** (target_voltage/target_current в charge_session.json), а не из дефолтов профиля; при сохранении сессии приоритет у фактических уставок прибора из HA.  
- Таймеры корректируются: вычитается **пауза потери связи**; при наличии сущности **uptime** прибора в HA общее время синхронизируется с таймером прибора (при каждом построении дашборда и в data_logger). Поддерживаются форматы: число секунд, строка «H:MM»/«M:SS», ISO-дата старта.  
- В «Полная инфо» выводится строка **Таймер прибора (sensor.rd_6018_uptime)** — для проверки, что возвращает интеграция.  
- Залитая ёмкость (А·ч) считается от сохранённых start_ah/stage_start_ah и текущего ah прибора.

---

## 📂 Структура проекта

```
├── bot.py           # Telegram-бот, дашборд, обработчики, вызов charge_logic
├── charge_logic.py  # FSM заряда, профили, триггеры, защиты
├── hass_api.py      # Клиент Home Assistant API
├── graphing.py      # Построение графиков
├── database.py      # SQLite, история для графиков
├── charging_log.py  # Лог событий (charging_history.log)
├── ai_engine.py     # DeepSeek
├── time_utils.py    # Часовой пояс пользователя
└── config.py        # Константы, ENTITY_MAP, MAX_VOLTAGE
```

---

## 📄 Лицензия и отказ от ответственности

Проект распространяется под лицензией **MIT**.  

**Использование на свой риск.** Программа поставляется «как есть». Авторы не несут ответственности за ущерб АКБ, оборудованию или имуществу. Работа с напряжением и током требует понимания правил безопасности и основ зарядки свинцово-кислотных АКБ. Не оставляйте заряд без присмотра на длительное время.
