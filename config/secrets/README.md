# Secrets

No tokens, passwords, encryption keys or certificates belong in this
directory. Use the existing environment/secrets mechanism. Configuration
files contain only `token_env` or `key_env` variable names.
