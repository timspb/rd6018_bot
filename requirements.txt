aiogram>=3.0
aiohttp>=3.8
# Keep CI and the supported Python 3.10+ deployment interpreter on one
# compatible API contract; 46.x is Python 3.11-only and creates matrix drift.
aioesphomeapi==36.0.1
aiosqlite>=0.19
matplotlib>=3.7
Pillow>=10.0
python-dotenv>=1.0
PyYAML>=6.0
pytz>=2023.3
