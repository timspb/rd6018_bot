aiogram>=3.0
aiohttp>=3.8
# Keep the direct ESPHome connector on the API contract validated against the
# deployed edge node.  Do not float this dependency.
aioesphomeapi==46.2.0
aiosqlite>=0.19
matplotlib>=3.7
Pillow>=10.0
python-dotenv>=1.0
PyYAML>=6.0
pytz>=2023.3
